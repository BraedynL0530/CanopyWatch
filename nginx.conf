server {
    listen 80;
    server_name braedyn.hackclub.app;

    # Frontend
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
    }

    # Backend
    location /api/ {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
    }
}